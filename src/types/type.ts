export interface CountryTypes {
    id: string;
    name: string;
    iso: string;
    currency: string;
    code: string;
    flagUrl: string;
    isDeprecated: boolean;
    createdAt: string;
    updatedAt: string;
  }